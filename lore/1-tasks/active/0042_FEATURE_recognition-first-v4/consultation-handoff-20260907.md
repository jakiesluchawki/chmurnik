# Consultation Handoff And Research Stop

## From Plan

The owner authorized preparation and private Drive storage, not contacting a
meteorologist. The owner also explicitly ended autonomous ML experiments.
No training, inference rerun, threshold tuning or candidate search was performed
to make these packages. This handoff does not satisfy the old model-quality goal.

## Delivered Private Material

`scripts/package-meteorologist-handoff.py` and
`scripts/build-consultation-workbook.mjs` reproducibly assemble the retained
evidence under ignored `.local/v4/consultation-20260907/`.

1. `CHMURNIK-01-OCENA-33-BEZ-PODPOWIEDZI.zip`: 42 entries, 33 licensed IMGW
   training-source photos with neutral R001-R033 identifiers, offline visual
   index, blank CSV and XLSX review forms, instructions, per-file provenance,
   license attribution and a future data protocol. These previously used
   training photos are a label-quality pilot, NOT a new independent benchmark.
2. `CHMURNIK-02-RAPORT-PO-OCENIE.zip`: six entries with the separate historical
   report, 22-case baseline/two-Astra comparison, source-label mapping and
   limitations, provenance hashes and future protocol. No retained predictions
   exist for the 33 pilot rows; those cells explicitly remain unavailable.
   The separate P001-P022 pilot is not presented as predictions for R001-R033.

Both ZIPs were uploaded to the owner's connected Drive and read back with
`shared=false` and exactly one owner permission. Private URLs and upload receipts
remain outside Git. No external recipient was added; nothing was sent to an
expert. No private feedback photographs/videos or uncertain-license comparison
images were included or published.

## Evidence Boundaries

- The 33 photos preserve the IMGW source attribution and CC BY 4.0 provenance.
  Identifiers and review forms do not reveal source labels or model opinions.
- The historical 22-case source-label agreement is 8/22 native, 3/22 Astra full
  image, 4/22 Astra center. These labels are not independently established truth,
  and the sessions do not reproduce the feedback author's ChatGPT environment.
- A retained candidate scored 77/123 versus 68/123 for the current model. Its
  paired improvement interval includes zero; it failed reliability requirements.
- 85.4% means 35/41 accepted calibration examples, with 41/260 coverage. It is
  neither overall recognition accuracy nor an independently verified test score.
- No candidate was shipped as an improved genus classifier.

## Emerged Decisions

1. Two separate archives prevent the report from cueing the initial review.
2. Add a blank, validated XLSX without changing the existing CSV contract. A
   second sheet explains ambiguous/mixed/unusable cases instead of forcing a name.
3. Retain numerical/textual records for the mixed-source pilot but omit its
   images where redistribution rights are not established.
4. Use an explicit missing-prediction declaration rather than rerun inference
   after the owner's experiment stop or fabricate a matched 33-photo comparison.

## Verification And Return Conditions

Both ZIP CRC checks pass (42 and 6 entries). The blind index passes at
320/390/1100px with all 33 neutral identifiers, full photos, offline links and
no external requests. Both workbook sheets were rendered and visually reviewed.
The report keeps source labels, model opinions and future expert judgments
separate. Archive hashes and private Drive metadata are retained locally.

The future protocol calls for new licensed observations, independent reviewers,
explicit uncertainty/multiple-cloud labels, adjudication, and grouping by
capture series/date/duplicates before partitioning train/validation/calibration/
test. Existing exposed sets stay exposed. Any new training or test campaign
requires the owner's new instruction; this document is not permission to resume.
